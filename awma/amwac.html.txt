<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="author" content="African Muslim Women's Association" />
  <meta name="description" content="Empowering Muslim women through faith, leadership, and community across Africa. Join AMWA for mentorship, health advocacy, and economic independence programs." />
  <meta name="keywords" content="African Muslim women, AMWA, women's empowerment, Islamic leadership, community development, health advocacy, economic independence" />
  <meta name="robots" content="index, follow, max-snippet:-1, max-image-preview:large, max-video-preview:-1" />
  <meta name="theme-color" content="#4a7c59" />
  <meta name="msapplication-TileColor" content="#4a7c59" />
  <meta name="apple-mobile-web-app-capable" content="yes" />
  <meta name="apple-mobile-web-app-status-bar-style" content="default" />
  <meta name="apple-mobile-web-app-title" content="AMWA" />

  <!-- Security Headers -->
  <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; img-src 'self' data: https:; font-src 'self' https://fonts.gstatic.com; connect-src 'self' https://formspree.io; frame-ancestors 'none'; base-uri 'self';" />
  <meta http-equiv="X-Content-Type-Options" content="nosniff" />
  <meta http-equiv="X-Frame-Options" content="DENY" />
  <meta http-equiv="X-XSS-Protection" content="1; mode=block" />
  <meta http-equiv="Referrer-Policy" content="strict-origin-when-cross-origin" />
  <meta http-equiv="Permissions-Policy" content="geolocation=(), microphone=(), camera=()" />

  <!-- Preload critical resources -->
  <link rel="preload" href="logo.jpg" as="image" type="image/jpeg" />
  <link rel="preload" href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" as="style" crossorigin />

  <!-- Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet" />

  <!-- SEO and Social -->
  <meta property="og:title" content="African Muslim Women's Association - Empowering Women Across Africa" />
  <meta property="og:description" content="Join AMWA for faith-based leadership, health advocacy, and economic empowerment programs supporting Muslim women throughout Africa." />
  <meta property="og:image" content="logo.jpg" />
  <meta property="og:url" content="https://amwa.org" />
  <meta property="og:type" content="website" />
  <meta property="og:site_name" content="African Muslim Women's Association" />
  <meta property="og:locale" content="en_US" />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content="African Muslim Women's Association" />
  <meta name="twitter:description" content="Empowering Muslim women through faith, leadership, and community across Africa." />
  <meta name="twitter:image" content="logo.jpg" />

  <link rel="canonical" href="https://amwa.org" />
  <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🕌</text></svg>" />
  <link rel="apple-touch-icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🕌</text></svg>" />

  <!-- Structured Data -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "Organization",
    "name": "African Muslim Women's Association",
    "url": "https://amwa.org",
    "logo": "https://amwa.org/logo.jpg",
    "description": "Empowering Muslim women through faith, leadership, and community across Africa.",
    "address": {
      "@type": "PostalAddress",
      "addressLocality": "Entebbe",
      "addressRegion": "Katabi",
      "addressCountry": "UG"
    },
    "contactPoint": {
      "@type": "ContactPoint",
      "telephone": "+256-757-102-598",
      "contactType": "customer service"
    },
    "sameAs": [
      "https://www.facebook.com/amwa.org",
      "https://www.instagram.com/amwa_org",
      "https://www.twitter.com/amwa_org"
    ]
  }
  </script>

  <title>African Muslim Women's Association - Empowering Women Across Africa</title>
  <style>
    :root {
      --background: #f8f9fa;
      --surface: rgba(255, 255, 255, 0.98);
      --surface-strong: #ffffff;
      --text: #2d3748;
      --text-secondary: #4a5568;
      --muted: #718096;
      --primary: #4a7c59;
      --primary-hover: #3a6b4a;
      --secondary: #e6d7c3;
      --accent: #8b7355;
      --border: rgba(74, 124, 89, 0.12);
      --border-hover: rgba(74, 124, 89, 0.2);
      --shadow: 0 10px 40px rgba(74, 124, 89, 0.08);
      --shadow-hover: 0 20px 60px rgba(74, 124, 89, 0.12);
      --success: #38a169;
      --error: #e53e3e;
      --warning: #d69e2e;
      --radius: 12px;
      --radius-sm: 8px;
      --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      --transition-fast: all 0.15s cubic-bezier(0.4, 0, 0.2, 1);
    }

    * {
      box-sizing: border-box;
    }

    html {
      scroll-behavior: smooth;
      -webkit-text-size-adjust: 100%;
    }

    body {
      margin: 0;
      min-height: 100vh;
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      font-weight: 400;
      font-size: 16px;
      line-height: 1.6;
      color: var(--text);
      background: linear-gradient(135deg, #f0f4f0 0%, #f8f9fa 50%, #fefefe 100%);
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }

    body::before {
      content: '';
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, rgba(255,255,255,0.4) 0%, transparent 40%);
      pointer-events: none;
      z-index: -1;
    }

    /* Accessibility */
    .sr-only {
      position: absolute;
      width: 1px;
      height: 1px;
      padding: 0;
      margin: -1px;
      overflow: hidden;
      clip: rect(0, 0, 0, 0);
      white-space: nowrap;
      border: 0;
    }

    .sr-status {
      position: absolute;
      left: -10000px;
      width: 1px;
      height: 1px;
      overflow: hidden;
    }

    /* Focus management */
    *:focus {
      outline: 2px solid var(--primary);
      outline-offset: 2px;
    }

    *:focus:not(:focus-visible) {
      outline: none;
    }

    *:focus-visible {
      outline: 2px solid var(--primary);
      outline-offset: 2px;
    }

    /* Skip link */
    .skip-link {
      position: absolute;
      top: -40px;
      left: 6px;
      background: var(--primary);
      color: white;
      padding: 8px;
      text-decoration: none;
      border-radius: var(--radius-sm);
      z-index: 1000;
      transition: var(--transition-fast);
    }

    .skip-link:focus {
      top: 6px;
    }
    nav {
      position: fixed;
      top: 0;
      width: 100%;
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border-bottom: 1px solid var(--border);
      z-index: 1000;
      padding: 1rem 1.5rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      box-shadow: var(--shadow);
      transition: var(--transition);
    }

    .logo-link {
      display: flex;
      align-items: center;
      text-decoration: none;
      transition: var(--transition);
    }

    .logo-link:hover, .logo-link:focus {
      transform: scale(1.05);
    }

    nav .logo-img {
      height: 40px;
      width: auto;
      display: block;
      max-width: 200px;
      transition: var(--transition);
    }

    nav ul {
      list-style: none;
      margin: 0;
      padding: 0;
      display: flex;
      gap: 2rem;
    }

    nav a {
      color: var(--text);
      text-decoration: none;
      font-weight: 500;
      padding: 0.5rem 1rem;
      border-radius: var(--radius-sm);
      transition: var(--transition);
      position: relative;
    }

    nav a:hover, nav a:focus {
      color: var(--primary);
      background: rgba(74, 124, 89, 0.1);
      outline: 2px solid var(--primary);
      outline-offset: 2px;
    }

    nav a::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 50%;
      width: 0;
      height: 2px;
      background: var(--primary);
      transition: var(--transition);
      transform: translateX(-50%);
    }

    nav a:hover::after, nav a:focus::after {
      width: 100%;
    }

    .hamburger {
      display: none;
      flex-direction: column;
      cursor: pointer;
      padding: 0.5rem;
      border-radius: var(--radius-sm);
      transition: var(--transition);
      background: transparent;
      border: none;
    }

    .hamburger:hover, .hamburger:focus {
      background: rgba(74, 124, 89, 0.1);
    }

    .hamburger span {
      width: 25px;
      height: 3px;
      background: var(--text);
      margin: 3px 0;
      transition: var(--transition);
      border-radius: 2px;
    }

    .hamburger[aria-expanded="true"] span:nth-child(1) {
      transform: rotate(-45deg) translate(-5px, 6px);
    }

    .hamburger[aria-expanded="true"] span:nth-child(2) {
      opacity: 0;
    }

    .hamburger[aria-expanded="true"] span:nth-child(3) {
      transform: rotate(45deg) translate(-5px, -6px);
    }
    header {
    position: relative;
    overflow: hidden;
    background: linear-gradient(135deg, #0A5C3B 0%, #1FAF6B 100%);
    color: white;
    padding: 7rem 1.5rem 5rem;
    text-align: center;
    margin-top: 0;
  }

  header:after {
    content: '';
    position: absolute;
    left: 50%;
    top: 0;
    width: 120%;
    height: 120%;
    background: radial-gradient(circle, rgba(255,255,255,0.14), transparent 55%);
    transform: translateX(-50%);
    pointer-events: none;
  }

  header h1 {
    position: relative;
    margin: 0 auto;
    max-width: 900px;
    font-size: clamp(2.6rem, 5vw, 4.4rem);
    letter-spacing: -0.02em;
    font-weight: 700;
    line-height: 1.05;
  }

  header p {
    position: relative;
    margin: 1.5rem auto 0;
    max-width: 760px;
    font-size: 1.15rem;
    opacity: 0.95;
  }

  .hero-footer {
    margin-top: 2rem;
    display: grid;
    gap: 1.25rem;
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .hero-footer .hero-item {
    padding: 1.4rem 1.25rem;
    border-radius: 20px;
    background: rgba(255,255,255,0.15);
    border: 1px solid rgba(255,255,255,0.22);
    color: white;
    backdrop-filter: blur(6px);
  }

  .hero-footer .hero-item strong {
    display: block;
    margin-bottom: 0.75rem;
    font-size: 1.05rem;
    font-weight: 600;
  }

  .hero-footer .hero-item span {
    color: rgba(255,255,255,0.88);
    font-size: 0.98rem;
    line-height: 1.6;
  }

  .cta-button {
    display: inline-block;
    background: white;
    color: #0A5C3B;
    padding: 0.85rem 1.75rem;
    border-radius: 50px;
    text-decoration: none;
    font-weight: 700;
    transition: all 0.3s ease;
    margin-top: 2rem;
  }

  .cta-button:hover,
  .cta-button:focus {
    background: #DFF5E8;
    color: #0A5C3B;
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(10, 92, 59, 0.25);
  }

    main {
      max-width: 1120px;
      margin: -3rem auto 3rem;
      padding: 0 1.5rem 2rem;
      display: grid;
      gap: 1.75rem;
    }
    .hero-panel,
    .section,
    .contact-card {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 28px;
      box-shadow: var(--shadow);
      backdrop-filter: blur(14px);
    }
    .hero-panel {
      padding: 2rem;
    }
    .section {
      padding: 2rem;
    }
    .section h2,
    .contact-card h2 {
      margin-top: 0;
      margin-bottom: 1rem;
      color: var(--primary);
      font-size: 1.9rem;
      font-weight: 600;
    }
    .section p,
    .section li,
    .contact-card p {
      font-size: 1rem;
      color: var(--muted);
    }
    .grid {
      display: grid;
      gap: 1.5rem;
    }
    .grid-2 {
      grid-template-columns: 1fr;
    }
    .slideshow-container {
      position: relative;
      max-width: 100%;
      height: 400px;
      margin: 0 auto;
      overflow: hidden;
      border-radius: 24px;
      box-shadow: var(--shadow);
    }
    .slide {
      display: block;
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      opacity: 0;
      transition: opacity 0.5s ease-in-out;
    }
    .slide img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
    }
    .slide-text {
      position: absolute;
      top: 20px;
      left: 20px;
      right: 20px;
      color: white;
      font-size: 2rem;
      font-weight: 600;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.7);
      white-space: nowrap;
      overflow: hidden;
      border-right: 2px solid white;
      animation: typewriter 3s steps(40, end) 1s forwards;
    }

    #newsletter-form {
      max-width: 400px;
      margin: 0 auto;
      display: flex;
      gap: 0.5rem;
      flex-wrap: wrap;
      align-items: flex-start;
    }

    .newsletter-input-wrapper {
      flex: 1 1 240px;
      position: relative;
      min-width: 0;
    }

    #newsletter-form input {
      width: 100%;
      padding: 0.75rem 1rem;
      border: 1px solid var(--border);
      border-radius: 8px;
      font-family: inherit;
      font-size: 1rem;
      background: var(--surface-strong);
      color: var(--text);
      box-sizing: border-box;
      transition: var(--transition);
    }

    #newsletter-form input:focus {
      outline: none;
      border-color: var(--primary);
      box-shadow: 0 0 0 3px rgba(74, 124, 89, 0.12);
    }

    #newsletter-form button {
      padding: 0.75rem 1.5rem;
      background: var(--primary);
      color: white;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: background 0.3s, opacity 0.3s;
      flex: 0 0 auto;
      min-width: 120px;
    }

    #newsletter-form button:hover,
    #newsletter-form button:focus {
      background: var(--accent);
    }

    .newsletter-input-wrapper .error-message {
      position: absolute;
      top: 100%;
      left: 0;
      right: 0;
      color: #e53e3e;
      font-size: 0.875rem;
      margin-top: 0.25rem;
      display: none;
    }

    .newsletter-input-wrapper.error .error-message {
      display: block;
    }

    .slide.active {
      opacity: 1;
    }
    .dots {
      text-align: center;
      padding: 1rem;
    }
    .dot {
      height: 12px;
      width: 12px;
      margin: 0 4px;
      background-color: var(--muted);
      border-radius: 50%;
      display: inline-block;
      transition: background-color 0.6s ease;
    }
    .dot.active {
      background-color: var(--primary);
    }
    .feature {
      padding: 1.5rem;
      border-radius: 22px;
      background: linear-gradient(160deg, rgba(74,124,89,0.08), rgba(255,255,255,0));
      border: 1px solid rgba(74,124,89,0.1);
    }
    .feature strong {
      display: block;
      margin-bottom: 0.85rem;
      color: var(--text);
      font-size: 1.05rem;
    }
    .feature span {
      display: block;
      color: var(--muted);
      margin-top: 0.35rem;
    }
    .objectives {
      list-style: none;
      padding: 0;
      margin: 0;
      display: grid;
      gap: 1rem;
    }
    .objectives li {
      display: grid;
      gap: 0.5rem;
      padding: 1.2rem 1.25rem;
      border-radius: 18px;
      background: rgba(74,124,89,0.05);
      border: 1px solid rgba(74,124,89,0.1);
      counter-increment: objective;
    }
    .objectives li::before {
      content: counter(objective);
      width: 2rem;
      height: 2rem;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      border-radius: 999px;
      background: var(--primary);
      color: white;
      font-weight: 700;
      margin-bottom: 0.25rem;
    }
    .rep-card {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 24px;
      padding: 1.5rem;
      text-align: center;
      box-shadow: var(--shadow);
      transition: all 0.3s ease;
    }
    .rep-card:hover {
      transform: translateY(-8px) scale(1.02);
      box-shadow: 0 40px 100px rgba(74, 124, 89, 0.15);
    }
    .rep-card img {
      width: 120px;
      height: 120px;
      border-radius: 50%;
      object-fit: cover;
      margin-bottom: 1rem;
      border: 3px solid var(--secondary);
    }
    .rep-card h3 {
      margin: 0 0 0.5rem;
      color: var(--primary);
      font-size: 1.2rem;
    }
    .rep-card .position {
      color: var(--muted);
      font-weight: 600;
      margin-bottom: 0.75rem;
    }
    .rep-card .contact {
      margin-bottom: 1rem;
    }
    .rep-card .contact a {
      color: var(--primary);
      font-weight: 500;
    }
    .rep-card .article-card {
      background: rgba(230, 215, 195, 0.3);
      border: 1px solid rgba(139, 115, 85, 0.15);
      border-radius: 16px;
      padding: 1rem;
      margin-top: 1rem;
      text-align: left;
    }
    .rep-card .article-card p {
      margin: 0;
      font-size: 0.9rem;
      color: var(--text);
    }
    .contact-card {
      padding: 1.75rem;
      text-align: left;
    }
    footer {
      text-align: center;
      color: var(--muted);
      font-size: 0.96rem;
      padding: 2rem 1rem 3rem;
      background: var(--surface);
      border-top: 1px solid var(--border);
    }
    footer a {
      color: var(--primary);
      text-decoration: none;
    }
    footer a:hover {
      text-decoration: underline;
    }
    @media (min-width: 768px) {
      nav ul {
        gap: 3rem;
      }
      main {
        gap: 2rem;
      }
      .grid-2 {
        grid-template-columns: 1fr 1fr;
      }
      .hero-footer {
        grid-template-columns: repeat(4, minmax(0, 1fr));
      }
      .section {
        padding: 4rem 2rem;
      }
      .join-form {
        max-width: 600px;
        margin: 0 auto;
      }
    }

    @media (max-width: 767px) {
      body {
        font-size: 14px;
      }

      nav {
        padding: 0.75rem 1rem;
      }

      nav .logo-img {
        height: 32px;
      }

      .hamburger {
        display: flex;
      }

      nav ul {
        position: absolute;
        top: 100%;
        left: 0;
        width: 100%;
        background: var(--surface-strong);
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
        flex-direction: column;
        padding: 1rem 0;
        box-shadow: var(--shadow);
        border-top: 1px solid var(--border);
        display: none;
        gap: 0;
      }

      nav ul.active {
        display: flex;
      }

      nav ul li {
        margin: 0;
        width: 100%;
        text-align: center;
      }

      nav a {
        display: block;
        padding: 1rem;
        margin: 0;
        border-radius: 0;
      }

      nav a:hover, nav a:focus {
        background: rgba(74, 124, 89, 0.1);
      }

      header {
        padding: 4rem 1rem 3rem;
        margin-top: 60px;
      }

      .section {
        padding: 2rem 1rem;
      }

      .slideshow-container {
        height: 250px;
      }

      .slide img {
        object-fit: cover;
      }

      #newsletter-form {
        flex-direction: column;
      }

      .newsletter-input-wrapper,
      #newsletter-form button {
        width: 100%;
      }

      .slide-text {
        font-size: 1.5rem;
        white-space: normal;
        border-right: none;
      }

      .join-form {
        padding: 1.5rem;
      }

      .form-group {
        margin-bottom: 1.5rem;
      }

      .partners-grid {
        grid-template-columns: repeat(2, 1fr);
        gap: 1rem;
      }

      .rep-card {
        padding: 1rem;
      }

      .rep-card img {
        width: 80px;
        height: 80px;
      }
    }

    @media (max-width: 480px) {
      .partners-grid {
        grid-template-columns: 1fr;
      }

      .hero-footer {
        grid-template-columns: 1fr;
      }

      nav ul {
        gap: 0;
      }
    }

    /* High contrast mode support */
    @media (prefers-contrast: high) {
      :root {
        --primary: #006400;
        --text: #000000;
        --background: #ffffff;
        --surface: #ffffff;
      }
    }

    /* Reduced motion support */
    @media (prefers-reduced-motion: reduce) {
      *,
      *::before,
      *::after {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
        scroll-behavior: auto !important;
      }
    }

    /* Loading states */
    .loading {
      position: relative;
      pointer-events: none;
      opacity: 0.7;
    }

    .loading::after {
      content: '';
      position: absolute;
      top: 50%;
      left: 50%;
      width: 20px;
      height: 20px;
      margin: -10px 0 0 -10px;
      border: 2px solid var(--primary);
      border-top-color: transparent;
      border-radius: 50%;
      animation: spin 1s linear infinite;
    }

    /* Image optimization */
    img {
      max-width: 100%;
      height: auto;
      display: block;
    }

    img[loading="lazy"] {
      transition: opacity 0.3s;
      opacity: 0;
    }

    img[loading="lazy"].loaded {
      opacity: 1;
    }

    /* Performance optimizations */
    .section {
      contain: layout style paint;
    }

    /* Better focus indicators */
    .focus-visible {
      outline: 2px solid var(--primary);
      outline-offset: 2px;
    }

    @media (max-width: 768px) {
      header {
        padding: 4rem 1rem 3rem;
      }
    }
    #back-to-top {
      position: fixed;
      bottom: 2rem;
      right: 2rem;
      background: var(--primary);
      color: white;
      border: none;
      border-radius: 50%;
      width: 3.5rem;
      height: 3.5rem;
      font-size: 1.4rem;
      font-weight: bold;
      cursor: pointer;
      opacity: 0;
      visibility: hidden;
      transition: var(--transition);
      z-index: 1001;
      box-shadow: var(--shadow);
      display: flex;
      align-items: center;
      justify-content: center;
      transform: scale(0.8);
    }

    #back-to-top.show {
      opacity: 1;
      visibility: visible;
      transform: scale(1);
    }

    #back-to-top:hover {
      background: var(--primary-hover);
      transform: scale(1.1);
      box-shadow: var(--shadow-hover);
    }

    #back-to-top:active {
      transform: scale(0.95);
    }
    .join-form {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 24px;
      padding: 2rem;
      max-width: 600px;
      margin: 0 auto;
      box-shadow: var(--shadow);
    }
    .join-form h2 {
      text-align: center;
      color: var(--primary);
      margin-bottom: 1.5rem;
    }
    .form-group {
      margin-bottom: 1.5rem;
    }

    .form-group label {
      display: block;
      margin-bottom: 0.5rem;
      font-weight: 600;
      color: var(--text);
      font-size: 0.95rem;
    }

    .form-group input,
    .form-group textarea,
    .form-group select {
      width: 100%;
      padding: 0.875rem 1rem;
      border: 2px solid var(--border);
      border-radius: var(--radius);
      font-size: 1rem;
      font-family: inherit;
      background: var(--surface-strong);
      color: var(--text);
      transition: var(--transition);
      box-sizing: border-box;
    }

    .form-group select {
      background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' fill='none' viewBox='0 0 20 20'%3e%3cpath stroke='%236b7280' stroke-linecap='round' stroke-linejoin='round' stroke-width='1.5' d='m6 8 4 4 4-4'/%3e%3c/svg%3e");
      background-position: right 0.75rem center;
      background-repeat: no-repeat;
      background-size: 1rem;
      padding-right: 2.5rem;
      cursor: pointer;
    }

    .form-group input:focus,
    .form-group textarea:focus,
    .form-group select:focus {
      outline: none;
      border-color: var(--primary);
      box-shadow: 0 0 0 3px rgba(74, 124, 89, 0.15);
      background: var(--surface-strong);
    }

    .form-group.error input,
    .form-group.error textarea,
    .form-group.error select {
      border-color: var(--error);
      box-shadow: 0 0 0 3px rgba(229, 62, 62, 0.15);
    }

    .form-group input::placeholder {
      color: var(--muted);
      opacity: 1;
    }

    .form-group textarea {
      resize: vertical;
      min-height: 100px;
    }

    .error-message {
      color: var(--error);
      font-size: 0.875rem;
      margin-top: 0.5rem;
      display: none;
      font-weight: 500;
      line-height: 1.4;
    }

    .form-group.error .error-message {
      display: block;
    }

    .join-btn {
      display: block;
      width: 100%;
      padding: 1rem 2rem;
      background: var(--primary);
      color: white;
      border: none;
      border-radius: var(--radius);
      font-size: 1.1rem;
      font-weight: 600;
      cursor: pointer;
      transition: var(--transition);
      position: relative;
      overflow: hidden;
      box-shadow: var(--shadow);
    }

    .join-btn::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
      transition: left 0.5s;
    }

    .join-btn:hover:not(:disabled)::before {
      left: 100%;
    }

    .join-btn:hover:not(:disabled) {
      background: var(--primary-hover);
      transform: translateY(-2px);
      box-shadow: var(--shadow-hover);
    }

    .join-btn:active:not(:disabled) {
      transform: translateY(0);
    }

    .join-btn:disabled {
      opacity: 0.7;
      cursor: not-allowed;
      transform: none;
      box-shadow: none;
    }

    .join-btn.loading {
      color: transparent;
    }

    .join-btn.loading::after {
      content: '';
      position: absolute;
      top: 50%;
      left: 50%;
      width: 20px;
      height: 20px;
      margin: -10px 0 0 -10px;
      border: 2px solid transparent;
      border-top-color: white;
      border-radius: 50%;
      animation: spin 1s linear infinite;
    }

    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }

    .join-btn {
      color: white;
      border: none;
      border-radius: 8px;
      font-size: 1rem;
      font-weight: 600;
      cursor: pointer;
      transition: background 0.3s;
    }
    .join-btn:hover {
      background: var(--accent);
    }
    .counter-section {
      text-align: center;
      padding: 2rem 0;
      background: linear-gradient(135deg, var(--primary), var(--secondary));
      color: white;
      margin: 2rem 0;
    }
    .counter {
      font-size: 3rem;
      font-weight: 700;
      margin: 0;
      display: inline-block;
    }
    .counter-label {
      font-size: 1.2rem;
      opacity: 0.9;
      margin-top: 0.5rem;
    }
    .partners-section {
      padding: 2rem 0;
    }
    .partners-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 1.5rem;
      max-width: 1000px;
      margin: 0 auto;
    }
    .partner-card {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 16px;
      padding: 1.5rem;
      text-align: center;
      box-shadow: var(--shadow);
      transition: transform 0.3s, box-shadow 0.3s;
    }
    .partner-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 20px 40px rgba(74, 124, 89, 0.15);
    }
    .partner-logo {
      width: 80px;
      height: 80px;
      background: var(--muted);
      border-radius: 50%;
      margin: 0 auto 1rem;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 2rem;
      color: var(--primary);
    }
    .partner-name {
      font-size: 1.2rem;
      font-weight: 700;
      text-transform: uppercase;
      color: var(--primary);
      margin-bottom: 0.5rem;
    }
    .partner-motto {
      font-style: italic;
      color: var(--muted);
      font-size: 0.95rem;
    }
  </style>
</head>
<body>
  <!-- Loading screen -->
  <div id="loading-screen" style="position: fixed; inset: 0; background: var(--surface-strong); display: flex; align-items: center; justify-content: center; z-index: 9999; transition: opacity 0.3s;">
    <div style="text-align: center;">
      <div style="width: 50px; height: 50px; border: 4px solid var(--border); border-top: 4px solid var(--primary); border-radius: 50%; animation: spin 1s linear infinite; margin: 0 auto 1rem;"></div>
      <p style="color: var(--text); font-weight: 500;">Loading AMWA...</p>
    </div>
  </div>

  <a href="#main" class="skip-link">Skip to main content</a>
  <nav role="navigation" aria-label="Main navigation">
    <a href="#main" class="logo-link" aria-label="African Muslim Women's Association home">
      <img src="logo.jpg" alt="African Muslim Women's Association logo" class="logo-img" />
    </a>
    <ul id="nav-menu" role="menubar">
      <li role="none"><a href="#about" role="menuitem">About</a></li>
      <li role="none"><a href="#objectives" role="menuitem">Objectives</a></li>
      <li role="none"><a href="#representatives" role="menuitem">Team</a></li>
      <li role="none"><a href="#contact" role="menuitem">Contact</a></li>
    </ul>
    <button class="hamburger" onclick="toggleMenu()" aria-expanded="false" aria-label="Toggle navigation menu">
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
    </button>
  </nav>
  <header>
    <h1>African Muslim Women's Association</h1>
    <p>Mentorship, leadership, entrepreneurship, and community upliftment for Muslim women across Africa.</p>
    <div class="hero-footer">
      <div class="hero-item">
        <strong>Faith &amp; Purpose</strong>
        <span>Supporting women through spiritual growth and moral leadership.</span>
      </div>
      <div class="hero-item">
        <strong>Economic Empowerment</strong>
        <span>Building financial confidence with business and investment platforms.</span>
      </div>
      <div class="hero-item">
        <strong>Community Unity</strong>
        <span>Creating partnerships, solidarity, and advocacy across African nations.</span>
      </div>
      <div class="hero-item">
        <strong>Health &amp; Wellbeing</strong>
        <span>Raising awareness for womens health, cancer, and maternal care.</span>
      </div>
    </div>
    <a href="#contact" class="cta-button">Get Involved</a>
  </header>
  <main id="main">
    <section id="about" class="hero-panel">
      <h2>About the association</h2>
      <p>The African Muslim Womens Association empowers Muslim women through mentorship, professional development, cross-border learning, and cultural celebration. We champion dignity, family stability, and active leadership while honoring Islamic values and African heritage.</p>
    </section>
    <section class="section">
      <h2>Visual Stories</h2>
      <p>Images can support our mission by showing community, training, and outreach in a welcoming, professional way. This slideshow demonstrates how photos fit naturally into the page.</p>
      <div class="slideshow-container" aria-label="Image slideshow showcasing AMWA activities">
        <div class="slide active">
          <img src="a1.jpg" alt="Empowering Women" loading="lazy" />
          <div class="slide-text">Empowering Women</div>
        </div>
        <div class="slide">
          <img src="a2.jpg" alt="Building Communities" loading="lazy" />
          <div class="slide-text">Building Communities</div>
        </div>
        <div class="slide">
          <img src="a3.jpg" alt="Leadership Training" loading="lazy" />
          <div class="slide-text">Leadership Training</div>
        </div>
        <div class="slide">
          <img src="a4.jpg" alt="Health & Wellness" loading="lazy" />
          <div class="slide-text">Health & Wellness</div>
        </div>
        <div class="slide">
          <img src="a5.jpg" alt="Economic Independence" loading="lazy" />
          <div class="slide-text">Economic Independence</div>
        </div>
      </div>
      <div class="dots">
        <span class="dot active" onclick="currentSlide(1)"></span>
        <span class="dot" onclick="currentSlide(2)"></span>
        <span class="dot" onclick="currentSlide(3)"></span>
        <span class="dot" onclick="currentSlide(4)"></span>
        <span class="dot" onclick="currentSlide(5)"></span>
      </div>
    </section>
    <section class="section grid grid-2">
      <div class="feature">
        <strong>Leadership &amp; Learning</strong>
        <span>Organizing seminars, conferences, and forums that develop leadership, confidence, and knowledge exchange.</span>
      </div>
      <div class="feature">
        <strong>Entrepreneurship &amp; Finance</strong>
        <span>Designing business platforms and financial literacy programs to support women entrepreneurs.</span>
      </div>
    </section>
    <section id="objectives" class="section">
      <h2>Our Objectives</h2>
      <ol class="objectives">
        <li><p>Provide faith-based mentorship and spiritual enrichment for Muslim women.</p></li>
        <li><p>Create business, investment, entrepreneurship, and partnership platforms to empower women economically.</p></li>
        <li><p>Facilitate seminars, conferences, and outdoor forums that promote leadership and knowledge exchange.</p></li>
        <li><p>Strengthen family and marriage stability through structured programs and community engagement.</p></li>
        <li><p>Promote modest fashion, dignity, and cultural identity in line with Islamic and African values.</p></li>
        <li><p>Increase representation of Muslim women in leadership and decision-making spaces.</p></li>
        <li><p>Build continental solidarity among Muslim women through unity, cooperation, and advocacy.</p></li>
        <li><p>Support women affected by cancer and obstetric fistula with education, advocacy, and healthcare partnerships.</p></li>
      </ol>
    </section>
    <section id="representatives" class="section">
      <h2>Our Representatives</h2>
      <div class="grid grid-2">
        <div class="rep-card">
          <center><img src="amwajazira.jpg" alt="Jazir Bashir Bulega"></center>
          <h3>JAZIR BASHIR BULEGA</h3>
          <p class="position">President</p>
          <div class="contact">
            <p>Email: <a href="mailto:jazirabashirbulega@gmail.com">jazirabashirbulega@gmail.com</a></p>
            <p>Phone: +256 761 354 929</p>
          </div>
          <div class="article-card">
            <p>"As President, I am committed to fostering unity and empowerment among Muslim women across Africa. Our faith guides us in building stronger communities and achieving economic independence."</p>
          </div>
        </div>
        <div class="rep-card">
          <center><img src="amwapro.jpg" alt="Pro Nazeera"></center>
          <h3>PRO NAZEERA</h3>
          <p class="position">Vice President</p>
          <div class="contact">
            <p>Email: <a href="mailto:prodlamini2023@gmail.com">prodlamini2023@gmail.com</a></p>
            <p>Phone: +268 728 2672</p>
          </div>
          <div class="article-card">
            <p>"Leadership starts with mentorship. At AMWA, we provide the tools and support needed for women to lead with confidence and integrity, honoring our cultural and religious values."</p>
          </div>
        </div>
        <div class="rep-card">
          <center><img src="amwasumaya.png" alt="Sumaya Kabayiza Latifah"></center>
          <h3>SUMAYA KABAYIZA LATIFAH</h3>
          <p class="position">Secretary General</p>
          <div class="contact">
            <p>Email: <a href="mailto:sumakeza@gmail.com">sumakeza@gmail.com</a></p>
            <p>Phone: +250 788 648 049</p>
          </div>
          <div class="article-card">
            <p>"Our work in health advocacy and family support is crucial. Together, we can create awareness and provide resources to uplift women facing challenges like cancer and maternal health issues."</p>
          </div>
        </div>
        <div class="rep-card">
          <center><img src="amwadesire.png" alt="Desire Mariam"></center>
          <h3>DESIRE MARIAM</h3>
          <p class="position">Treasurer</p>
          <div class="contact">
            <p>Email: <a href="mailto:desiremariam@gmail.com">desiremariam@gmail.com</a></p>
            <p>Phone: +244 951 678 617</p>
          </div>
          <div class="article-card">
            <p>"Financial literacy and entrepreneurship are key to our mission. Through our programs, we equip women with the knowledge to build sustainable businesses and secure their futures."</p>
          </div>
        </div>
      </div>
    </section>
    <section id="contact" class="contact-card">
      <h2>Contact</h2>
      <p>For partnership inquiries, event support, or program questions, please contact us at:</p>
      <p><a href="mailto:africanmuslimwomensassociation@gmail.com">africanmuslimwomensassociation@gmail.com</a></p>
      <p>Phone: +256 757 102 598 / +256 761 354 929</p>
      <p>Address: Entebbe, Katabi, Uganda</p>
    </section>
    <section class="section">
      <div class="join-form">
        <h2 id="join-heading">Join Our Community</h2>
        <form id="join-form" action="https://formspree.io/f/mykljanr" method="POST" role="form" aria-labelledby="join-heading">
          <input type="hidden" name="_subject" value="New AMWA Membership Submission">
          <input type="hidden" name="_to" value="kevmulgeo@gmail.com">
          <input type="hidden" name="_replyto" id="replyto-field" value="">
          <input type="hidden" name="_next" value="">
          <div id="join-status" class="sr-status" aria-live="polite" aria-atomic="true"></div>
          <div class="form-group">
            <label for="name">Full Name</label>
            <input type="text" id="name" name="name" required minlength="2" maxlength="100" autocomplete="name">
            <div class="error-message">Please enter a valid name (2-100 characters)</div>
          </div>
          <div class="form-group">
            <label for="email">Email Address</label>
            <input type="email" id="email" name="email" required autocomplete="email" pattern="^[^\s@]+@[^\s@]+\.[^\s@]+$">
            <div class="error-message">Please enter a valid email address</div>
          </div>
          <div class="form-group">
            <label for="country-code">Country Code</label>
            <select id="country-code" name="country-code" required>
              <option value="" disabled selected>Select country code</option>
              <option value="+20">Egypt +20</option>
              <option value="+211">South Sudan +211</option>
              <option value="+212">Morocco +212</option>
              <option value="+213">Algeria +213</option>
              <option value="+216">Tunisia +216</option>
              <option value="+218">Libya +218</option>
              <option value="+220">Gambia +220</option>
              <option value="+221">Senegal +221</option>
              <option value="+222">Mauritania +222</option>
              <option value="+223">Mali +223</option>
              <option value="+224">Guinea +224</option>
              <option value="+225">Ivory Coast +225</option>
              <option value="+226">Burkina Faso +226</option>
              <option value="+227">Niger +227</option>
              <option value="+228">Togo +228</option>
              <option value="+229">Benin +229</option>
              <option value="+230">Mauritius +230</option>
              <option value="+231">Liberia +231</option>
              <option value="+232">Sierra Leone +232</option>
              <option value="+233">Ghana +233</option>
              <option value="+234">Nigeria +234</option>
              <option value="+235">Chad +235</option>
              <option value="+236">Central African Republic +236</option>
              <option value="+237">Cameroon +237</option>
              <option value="+238">Cape Verde +238</option>
              <option value="+239">Sao Tome &amp; Principe +239</option>
              <option value="+240">Equatorial Guinea +240</option>
              <option value="+241">Gabon +241</option>
              <option value="+242">Republic of the Congo +242</option>
              <option value="+243">DR Congo +243</option>
              <option value="+244">Angola +244</option>
              <option value="+245">Guinea-Bissau +245</option>
              <option value="+246">British Indian Ocean Territory +246</option>
              <option value="+248">Seychelles +248</option>
              <option value="+249">Sudan +249</option>
              <option value="+250">Rwanda +250</option>
              <option value="+251">Ethiopia +251</option>
              <option value="+252">Somalia +252</option>
              <option value="+253">Djibouti +253</option>
              <option value="+254">Kenya +254</option>
              <option value="+255">Tanzania +255</option>
              <option value="+256">Uganda +256</option>
              <option value="+257">Burundi +257</option>
              <option value="+258">Mozambique +258</option>
              <option value="+260">Zambia +260</option>
              <option value="+261">Madagascar +261</option>
              <option value="+262">Reunion +262</option>
              <option value="+263">Zimbabwe +263</option>
              <option value="+264">Namibia +264</option>
              <option value="+265">Malawi +265</option>
              <option value="+266">Lesotho +266</option>
              <option value="+267">Botswana +267</option>
              <option value="+268">Eswatini +268</option>
              <option value="+269">Comoros +269</option>
              <option value="+27">South Africa +27</option>
              <option value="+290">Saint Helena +290</option>
              <option value="+291">Eritrea +291</option>
              <option value="+297">Aruba +297</option>
              <option value="+298">Faroe Islands +298</option>
              <option value="+299">Greenland +299</option>
            </select>
            <div class="error-message">Please select your country code</div>
          </div>
          <div class="form-group">
            <label for="phone">Mobile Number</label>
            <input type="tel" id="phone" name="phone" placeholder="Enter local mobile number" pattern="^[0-9\s\-\(\)]{7,20}$" maxlength="20" autocomplete="tel">
            <div class="error-message">Please enter a valid mobile number</div>
          </div>
          <div class="form-group">
            <label for="message">Why do you want to join?</label>
            <textarea id="message" name="message" rows="4" maxlength="500"></textarea>
            <div class="error-message">Please share why you want to join (max 500 characters)</div>
          </div>
          <button type="submit" class="join-btn">Join AMWA</button>
        </form>
      </div>
    </section>
    <section class="counter-section">
      <div>
        <div class="counter" id="member-counter">0</div>
        <div class="counter-label">Active Members</div>
      </div>
    </section>
    <section class="partners-section">
      <h2 style="text-align: center; color: var(--primary); margin-bottom: 2rem;">Our Partners</h2>
      <div class="partners-grid">
        <div class="partner-card">
          <div class="partner-logo">🏛️</div>
          <div class="partner-name">AFRICAN UNION</div>
          <div class="partner-motto">"Unity in Diversity"</div>
        </div>
        <div class="partner-card">
          <div class="partner-logo">🌍</div>
          <div class="partner-name">UN WOMEN</div>
          <div class="partner-motto">"Empowering Women, Empowering Humanity"</div>
        </div>
        <div class="partner-card">
          <div class="partner-logo">🤝</div>
          <div class="partner-name">OIC WOMEN</div>
          <div class="partner-motto">"Strength in Unity"</div>
        </div>
        <div class="partner-card">
          <div class="partner-logo">💼</div>
          <div class="partner-name">AFRICAN DEVELOPMENT BANK</div>
          <div class="partner-motto">"Building Prosperity for Africa"</div>
        </div>
      </div>
    </section>
    <section class="section">
      <h2 style="text-align: center; color: var(--primary); margin-bottom: 2rem;">What Our Members Say</h2>
      <div class="grid grid-2">
        <div class="feature" style="text-align: center;">
          <blockquote style="font-style: italic; color: var(--muted); margin-bottom: 1rem;">
            "AMWA has transformed my life. Through their programs, I've gained confidence and skills to lead in my community."
          </blockquote>
          <cite style="font-weight: 600; color: var(--primary);">— Fatima K., Nairobi</cite>
        </div>
        <div class="feature" style="text-align: center;">
          <blockquote style="font-style: italic; color: var(--muted); margin-bottom: 1rem;">
            "The mentorship and support from AMWA sisters has been invaluable. We've built lasting friendships and achieved so much together."
          </blockquote>
          <cite style="font-weight: 600; color: var(--primary);">— Amina M., Lagos</cite>
        </div>
      </div>
    </section>
    <section class="section" style="background: var(--background); padding: 2rem; border-radius: 24px; margin: 2rem 0;">
      <h2 style="text-align: center; color: var(--primary); margin-bottom: 1rem;">Stay Connected</h2>
      <p style="text-align: center; color: var(--muted); margin-bottom: 1.5rem;">Subscribe to our newsletter for updates on events, programs, and community news.</p>
      <form id="newsletter-form" role="form" aria-labelledby="newsletter-heading">
        <div id="newsletter-status" class="sr-status" aria-live="polite" aria-atomic="true"></div>
        <div class="newsletter-input-wrapper">
          <input type="email" id="newsletter-email" name="newsletter-email" placeholder="Your email address" required>
          <div class="error-message" id="newsletter-error" style="position: absolute; top: 100%; left: 0; right: 0; color: #e53e3e; font-size: 0.875rem; margin-top: 0.25rem; display: none;">Please enter a valid email address</div>
        </div>
        <button type="submit" id="newsletter-btn">Subscribe</button>
      </form>
    </section>
  <button id="back-to-top" aria-label="Back to top">↑</button>
  <footer>
    <p>&copy; 2026 African Muslim Women's Association. Empowering women with faith, leadership, and solidarity.</p>
    <p><a href="#about">Privacy Policy</a> | <a href="#about">Terms of Service</a> | <a href="https://www.facebook.com/amwa" target="_blank">Facebook</a> | <a href="https://www.twitter.com/amwa" target="_blank">Twitter</a></p>
  </footer>
  <script>
    function toggleMenu() {
      const menu = document.getElementById('nav-menu');
      const hamburger = document.querySelector('.hamburger');
      const isExpanded = hamburger.getAttribute('aria-expanded') === 'true';

      menu.classList.toggle('active');
      hamburger.setAttribute('aria-expanded', !isExpanded);

      // Close menu when clicking outside
      if (!isExpanded) {
        document.addEventListener('click', closeMenuOnClickOutside);
        document.addEventListener('keydown', closeMenuOnEscape);
      } else {
        document.removeEventListener('click', closeMenuOnClickOutside);
        document.removeEventListener('keydown', closeMenuOnEscape);
      }
    }

    function closeMenuOnClickOutside(e) {
      const menu = document.getElementById('nav-menu');
      const hamburger = document.querySelector('.hamburger');

      if (!menu.contains(e.target) && !hamburger.contains(e.target)) {
        menu.classList.remove('active');
        hamburger.setAttribute('aria-expanded', 'false');
        document.removeEventListener('click', closeMenuOnClickOutside);
        document.removeEventListener('keydown', closeMenuOnEscape);
      }
    }

    function closeMenuOnEscape(e) {
      if (e.key === 'Escape') {
        const menu = document.getElementById('nav-menu');
        const hamburger = document.querySelector('.hamburger');

        menu.classList.remove('active');
        hamburger.setAttribute('aria-expanded', 'false');
        document.removeEventListener('click', closeMenuOnClickOutside);
        document.removeEventListener('keydown', closeMenuOnEscape);
      }
    }

    let slideIndex = 1;
    showSlides(slideIndex);

    function plusSlides(n) {
      showSlides(slideIndex += n);
    }

    function currentSlide(n) {
      showSlides(slideIndex = n);
    }

    function showSlides(n) {
      let i;
      let slides = document.getElementsByClassName("slide");
      let dots = document.getElementsByClassName("dot");
      if (n > slides.length) {slideIndex = 1}
      if (n < 1) {slideIndex = slides.length}
      for (i = 0; i < slides.length; i++) {
        slides[i].classList.remove("active");
      }
      for (i = 0; i < dots.length; i++) {
        dots[i].className = dots[i].className.replace(" active", "");
      }
      slides[slideIndex-1].classList.add("active");
      dots[slideIndex-1].className += " active";
      // Reset typewriter animation
      let text = slides[slideIndex-1].querySelector('.slide-text');
      let textLength = text.textContent.replace(/\s/g, '').length;
      text.style.animation = 'none';
      setTimeout(() => {
        text.style.animation = `typewriter 3s steps(${textLength}, end) 1s forwards`;
      }, 10);
    }

    let slideInterval;

    function startSlideshow() {
      slideInterval = setInterval(() => {
        plusSlides(1);
      }, 5000);
    }

    function stopSlideshow() {
      clearInterval(slideInterval);
    }

    startSlideshow();

    // Pause on hover
    const slideshowContainer = document.querySelector('.slideshow-container');
    slideshowContainer.addEventListener('mouseenter', stopSlideshow);
    slideshowContainer.addEventListener('mouseleave', startSlideshow);

    // Back to top button
    const backToTopBtn = document.getElementById('back-to-top');
    window.addEventListener('scroll', () => {
      if (window.pageYOffset > 300) {
        backToTopBtn.classList.add('show');
      } else {
        backToTopBtn.classList.remove('show');
      }
    });
    backToTopBtn.addEventListener('click', () => {
      window.scrollTo({ top: 0, behavior: 'smooth' });
    });

    // Service Worker registration for caching
    if ('serviceWorker' in navigator) {
      window.addEventListener('load', () => {
        navigator.serviceWorker.register('/sw.js')
          .then(registration => console.log('SW registered'))
          .catch(error => console.log('SW registration failed'));
      });
    }

    // Lazy loading for images
    function lazyLoadImages() {
      const images = document.querySelectorAll('img[loading="lazy"]');
      const imageObserver = new IntersectionObserver((entries, observer) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            const img = entry.target;
            img.classList.add('loaded');
            observer.unobserve(img);
          }
        });
      });

      images.forEach(img => imageObserver.observe(img));
    }

    // Performance monitoring
    function logPerformance() {
      if ('performance' in window && 'getEntriesByType' in performance) {
        window.addEventListener('load', () => {
          setTimeout(() => {
            const perfData = performance.getEntriesByType('navigation')[0];
            console.log('Page load time:', perfData.loadEventEnd - perfData.fetchStart, 'ms');
          }, 0);
        });
      }
    }

    // Initialize lazy loading and performance monitoring
    lazyLoadImages();
    logPerformance();

    // Keyboard navigation for slideshow
    document.addEventListener('keydown', (e) => {
      if (e.key === 'ArrowLeft') {
        plusSlides(-1);
      } else if (e.key === 'ArrowRight') {
        plusSlides(1);
      }
    });

    // Touch/swipe support for mobile
    let touchStartX = 0;
    let touchEndX = 0;

    slideshowContainer.addEventListener('touchstart', (e) => {
      touchStartX = e.changedTouches[0].screenX;
    });

    slideshowContainer.addEventListener('touchend', (e) => {
      touchEndX = e.changedTouches[0].screenX;
      handleSwipe();
    });

    function handleSwipe() {
      const swipeThreshold = 50;
      if (touchEndX < touchStartX - swipeThreshold) {
        plusSlides(1); // Swipe left - next slide
      } else if (touchEndX > touchStartX + swipeThreshold) {
        plusSlides(-1); // Swipe right - previous slide
      }
    }

    // Hide loading screen
    window.addEventListener('load', () => {
      const loadingScreen = document.getElementById('loading-screen');
      if (loadingScreen) {
        setTimeout(() => {
          loadingScreen.style.opacity = '0';
          setTimeout(() => {
            loadingScreen.style.display = 'none';
          }, 300);
        }, 500);
      }
    });
    const linkPreload = document.createElement('link');
    linkPreload.rel = 'preload';
    linkPreload.href = 'logo.jpg';
    linkPreload.as = 'image';
    document.head.appendChild(linkPreload);

    // Animated counter
    function animateCounter(element, target, duration = 2000) {
      let start = 0;
      const increment = target / (duration / 16);
      const timer = setInterval(() => {
        start += increment;
        if (start >= target) {
          element.textContent = target.toLocaleString();
          clearInterval(timer);
        } else {
          element.textContent = Math.floor(start).toLocaleString();
        }
      }, 16);
    }

    // Start counter animation when in view
    const counterElement = document.getElementById('member-counter');
    const counterObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          animateCounter(counterElement, 1234); // Example number
          counterObserver.unobserve(entry.target);
        }
      });
    });
    counterObserver.observe(counterElement);

    // Form validation and security functions
    function sanitizeInput(input) {
      return input.replace(/[<>]/g, '').trim();
    }

    function validateEmail(email) {
      const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
      return emailRegex.test(email);
    }

    function titleCaseName(name) {
      return name
        .toLowerCase()
        .split(' ')
        .filter(Boolean)
        .map(word => word.charAt(0).toUpperCase() + word.slice(1))
        .join(' ');
    }

    function validatePhone(phone) {
      const phoneRegex = /^[0-9\s\-\(\)]{7,20}$/;
      return phoneRegex.test(phone) || phone === '';
    }

    function showError(field, message) {
      const formGroup = field.closest('.form-group');
      const errorMsg = formGroup.querySelector('.error-message');
      formGroup.classList.add('error');
      if (errorMsg) errorMsg.textContent = message;
    }

    function clearError(field) {
      const formGroup = field.closest('.form-group');
      formGroup.classList.remove('error');
    }

    function validateForm(form) {
      let isValid = true;
      const fields = form.querySelectorAll('input, textarea, select');

      fields.forEach(field => {
        clearError(field);
        const value = sanitizeInput(field.value);

        switch(field.name) {
          case 'name':
            if (value.length < 2 || value.length > 100) {
              showError(field, 'Name must be 2-100 characters');
              isValid = false;
            }
            break;
          case 'email':
            if (!validateEmail(value)) {
              showError(field, 'Please enter a valid email address');
              isValid = false;
            }
            break;
          case 'country-code':
            if (!value) {
              showError(field, 'Please select your country code');
              isValid = false;
            }
            break;
          case 'phone':
            if (!value) {
              showError(field, 'Please enter your mobile number');
              isValid = false;
            } else if (!validatePhone(value)) {
              showError(field, 'Please enter a valid mobile number');
              isValid = false;
            }
            break;
          case 'message':
            if (value.length > 500) {
              showError(field, 'Message must be less than 500 characters');
              isValid = false;
            }
            break;
        }
      });

      return isValid;
    }

    // Rate limiting simulation
    const submissionTimes = new Map();

    function checkRateLimit(formType, limit = 5, windowMs = 60000) {
      const now = Date.now();
      const key = formType;
      const times = submissionTimes.get(key) || [];

      // Remove old submissions outside the window
      const validTimes = times.filter(time => now - time < windowMs);
      submissionTimes.set(key, validTimes);

      if (validTimes.length >= limit) {
        return false; // Rate limit exceeded
      }

      validTimes.push(now);
      submissionTimes.set(key, validTimes);
      return true;
    }

    // Form submission with enhanced security
    const joinForm = document.getElementById('join-form');
    const joinBtn = joinForm.querySelector('.join-btn');
    const nameInput = document.getElementById('name');
    const countryCodeInput = document.getElementById('country-code');
    const phoneInput = document.getElementById('phone');

    const newsletterForm = document.getElementById('newsletter-form');
    const newsletterEmail = document.getElementById('newsletter-email');
    const newsletterError = document.getElementById('newsletter-error');
    const newsletterBtn = document.getElementById('newsletter-btn');

    const phonePlaceholders = {
      '+20': '10 1234 5678',
      '+211': '0912 345 678',
      '+212': '0612 345 678',
      '+213': '0612 345 678',
      '+216': '20 123 456',
      '+218': '0912 345 678',
      '+220': '3012 345 678',
      '+221': '70 123 4567',
      '+222': '30 123 4567',
      '+223': '65 12 34 56',
      '+224': '60 123 4567',
      '+225': '01 23 45 67 89',
      '+226': '70 12 34 56',
      '+227': '90 12 34 56',
      '+228': '90 12 34 56',
      '+229': '90 12 34 56',
      '+230': '5 123 4567',
      '+231': '077 123 4567',
      '+232': '076 123 4567',
      '+233': '024 123 4567',
      '+234': '081 2345 6789',
      '+235': '63 12 34 56',
      '+236': '05 12 34 56',
      '+237': '6 12 34 56 78',
      '+238': '99 123 456',
      '+239': '99 123 456',
      '+240': '222 123 456',
      '+241': '06 12 34 56 78',
      '+242': '06 123 4567',
      '+243': '081 234 5678',
      '+244': '921 123 456',
      '+245': '651 234 567',
      '+246': '650 123 456',
      '+248': '2 123 456',
      '+249': '091 123 4567',
      '+250': '07 012 3456',
      '+251': '09 012 3456',
      '+252': '61 123 4567',
      '+253': '77 123 456',
      '+254': '712 345 678',
      '+255': '071 234 5678',
      '+256': '077 123 4567',
      '+257': '79 123 4567',
      '+258': '82 123 4567',
      '+260': '095 123 4567',
      '+261': '032 12 345 67',
      '+262': '692 12 34 56',
      '+263': '0712 345 678',
      '+264': '081 123 4567',
      '+265': '088 123 4567',
      '+266': '062 123 456',
      '+267': '72 123 456',
      '+268': '76 123 456',
      '+269': '32 123 456',
      '+27': '071 123 4567',
      '+290': '44 1234',
      '+291': '7 123 456',
      '+297': '56 123 456',
      '+298': '34 123',
      '+299': '34 123'
    };

    function updatePhonePlaceholder(code) {
      const hint = phonePlaceholders[code] || '123 456 7890';
      phoneInput.placeholder = `Example: ${hint}`;
    }

    countryCodeInput.addEventListener('change', () => {
      updatePhonePlaceholder(countryCodeInput.value);
    });

    updatePhonePlaceholder(countryCodeInput.value);

    nameInput.addEventListener('blur', () => {
      nameInput.value = titleCaseName(nameInput.value);
    });

    joinForm.addEventListener('submit', (e) => {
      // Let the form submit normally to Formspree
      nameInput.value = titleCaseName(nameInput.value);

      if (!validateForm(joinForm)) {
        e.preventDefault();
        return;
      }

      if (!checkRateLimit('join')) {
        e.preventDefault();
        alert('Too many submissions. Please wait before trying again.');
        return;
      }

      // Set the reply-to field before submission
      document.getElementById('replyto-field').value = document.getElementById('email').value;
      // Set redirect back to this page with success parameter
      document.querySelector('input[name="_next"]').value = window.location.href + '?submitted=true';

      joinBtn.disabled = true;
      joinBtn.textContent = 'Submitting...';

      // Form will submit normally to Formspree
    });

    // Check for success parameter on page load
    if (window.location.search.includes('submitted=true')) {
      const successMsg = document.createElement('div');
      successMsg.textContent = 'Thank you! Your membership request has been sent. You should receive a confirmation email soon.';
      successMsg.style.cssText = 'color: var(--primary); font-weight: 600; margin: 1rem 0; padding: 1rem; background: rgba(74, 124, 89, 0.1); border-radius: 8px; line-height: 1.5;';
      document.querySelector('.join-form').prepend(successMsg);

      // Clean up URL
      history.replaceState(null, null, window.location.pathname);

      setTimeout(() => {
        successMsg.remove();
      }, 10000);
    }

    newsletterForm.addEventListener('submit', async (e) => {
      e.preventDefault();

      const email = sanitizeInput(newsletterEmail.value);
      newsletterError.style.display = 'none';

      if (!validateEmail(email)) {
        newsletterError.style.display = 'block';
        return;
      }

      if (!checkRateLimit('newsletter', 3, 300000)) { // 3 per 5 minutes
        newsletterError.textContent = 'Too many requests. Please wait before subscribing again.';
        newsletterError.style.display = 'block';
        return;
      }

      newsletterBtn.disabled = true;
      newsletterBtn.textContent = 'Subscribing...';

      try {
        console.log('Newsletter signup:', email);

        await new Promise(resolve => setTimeout(resolve, 800));

        const successMsg = document.createElement('div');
        successMsg.textContent = 'Successfully subscribed! Check your email for confirmation.';
        successMsg.style.cssText = 'color: var(--primary); font-weight: 600; margin-top: 0.5rem;';
        newsletterForm.appendChild(successMsg);

        // Screen reader announcement
        const statusDiv = document.getElementById('newsletter-status');
        statusDiv.textContent = 'Successfully subscribed to newsletter.';

        newsletterForm.reset();
        setTimeout(() => {
          successMsg.remove();
          statusDiv.textContent = '';
        }, 3000);

      } catch (error) {
        console.error('Newsletter signup error:', error);
        newsletterError.textContent = 'An error occurred. Please try again.';
        newsletterError.style.display = 'block';
      } finally {
        newsletterBtn.disabled = false;
        newsletterBtn.textContent = 'Subscribe';
      }
    });
  </script>
</body>
</html>
 